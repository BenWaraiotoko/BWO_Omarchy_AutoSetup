# BWO Extra Packages for Omarchy
# -------------------------------------------------------------
# These are ADDITIONAL packages on top of what Omarchy installs
# by default. Omarchy already includes: bat, btop, eza, fzf,
# fastfetch, github-cli, jq, lazygit, neovim/omarchy-nvim,
# obsidian, ripgrep, spotify, tmux, 1password, 1password-cli,
# claude-code, docker, lazydocker, etc.
#
# Format: one package name per line (pacman/AUR package names)
# Comments start with #
#
# All packages are from the official Arch extra repo unless
# marked (AUR).

# -------------------------------------------------------------
# CLI Tools & Utilities
# -------------------------------------------------------------
lazysql           # Terminal SQL client TUI (AUR)
go-yq             # YAML processor, like jq for YAML (package name is go-yq, binary is yq)
git-delta         # Fancy git diff viewer (package name is git-delta, binary is delta)
csvlens           # Interactive CSV viewer in terminal
glow              # Markdown renderer for terminal
chafa             # Render images in terminal
ranger            # VIM-inspired terminal file manager

# -------------------------------------------------------------
# Languages & Runtimes
# (Omarchy includes ruby, rust; add what's missing)
# -------------------------------------------------------------
python-pipx       # Install Python CLI tools in isolated envs
nodejs            # Node.js runtime
npm               # Node package manager
nvm               # Node Version Manager

# -------------------------------------------------------------
# Cloud & DevOps
# -------------------------------------------------------------
aws-cli-v2        # AWS CLI
kubectl           # Kubernetes CLI
tailscale         # Secure mesh VPN

# -------------------------------------------------------------
# GUI Applications
# (Omarchy includes: obsidian, spotify, chromium, alacritty,
#  1password, signal-desktop, obs-studio, libreoffice)
# -------------------------------------------------------------
ghostty               # GPU-accelerated terminal emulator
zed                   # Fast, collaborative code editor
discord               # Chat platform
proton-vpn-gtk-app    # Proton VPN client
protonmail-bridge     # Proton Mail bridge
postman-bin           # API testing (AUR)
notion-app-electron   # Notion (AUR)
# resilio-sync        # P2P file sync — install manually if needed

# -------------------------------------------------------------
# AI / ML Tools
# -------------------------------------------------------------
ollama            # Run local LLMs
# claude-code     # Already in Omarchy base

# -------------------------------------------------------------
# GNU Utilities
# -------------------------------------------------------------
stow              # Symlink farm manager
